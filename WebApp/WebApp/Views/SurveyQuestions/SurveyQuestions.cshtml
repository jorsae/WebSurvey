@model IEnumerable<WebApp.Models.SurveyQuestion>

@{
    ViewBag.Title = "Survey Questions";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Survey.SurveyTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Question)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QuestionNumber)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Survey.SurveyTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Question)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuestionNumber)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.SurveyQuestionId }) |
                @Html.ActionLink("Details", "Details", new { id = item.SurveyQuestionId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.SurveyQuestionId })
            </td>
        </tr>
    }

</table>
